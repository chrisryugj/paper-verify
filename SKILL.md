---
name: paper-verify
description: >
  논문·보고서의 인용(법령·판례·학술문헌·웹출처)을 실제 원본과 대조해
  실존·일치 여부를 검증하는 스킬. 법령은 k-skill 법제처 프록시, 학술은
  CrossRef·OpenAlex·Semantic Scholar·KCI·RISS, 웹은 insane-search로 라우팅한다.
  AI가 지어낸 환각 인용, 개정돼서 달라진 조문, 연도·저자 오기를 잡는다.
  탐색 모드로 "이 주장 근거 찾아줘"도 처리한다.
  트리거 — "논문 검증", "출처 검증해줘", "참고문헌 확인", "인용 검증",
  "각주 진짜 있는 건지 확인", "법령 인용 맞는지", "이 조문 현행인지",
  "환각 인용 잡아줘", "근거 문헌 찾아줘", "선행연구 찾아줘",
  verify citations, check references, hallucinated citations.
  단순 법령 1건 조회는 법령 MCP/프록시 직접 사용, 맞춤법·문체는 별도 스킬.
---

# Paper Verify — 논문 인용 검증

> 인용을 추출 → 유형별 라우팅 → 원본 대조 → 3단 판정 리포트.

## 파이프라인

### 0. 입력 준비
- 텍스트/마크다운: 그대로 진행
- PDF: Read 도구로 직접
- HWP: `hwp-to-hwpx` 스킬로 변환 → HWPX는 zip이므로 `unzip -p file.hwpx Contents/section0.xml`로 텍스트 추출
- 참고문헌 목록만 던져줘도 동작
- **피인용 논문 원문 PDF도 함께 주면** → 전문 대조 모드 활성화(아래 §전문 대조). 국문 학술논문(KCI 등)은 본문이 유료벽·API 미제공이라, 페이지 단위 내용 검증은 사용자가 그 PDF를 확보해 넣어줘야만 가능

### 1. 인용 추출 · 분류

본문과 참고문헌에서 아래 패턴으로 인용을 뽑아 유형을 붙인다.

| 유형 | 패턴 예 | 감지 힌트 |
|------|---------|-----------|
| 법령 | 「지방재정법」 제38조제1항 | `「?[가-힣·\s]+(법|법률|시행령|시행규칙|조례|규칙)」?\s*제\d+조(의\d+)?(제\d+항)?` |
| 판례 | 대법원 2023. 5. 18. 선고 2019두38656 판결 | 사건번호 `\d{2,4}(다|두|도|누|허|헌마|헌바|헌가|카|므|재)\d+` |
| 학술 | 김OO(2021), Smith & Lee(2019), doi:10.xxxx | 저자(연도) 패턴, DOI `10\.\d{4,9}/\S+` |
| 웹/보도 | URL, ○○부 보도자료(2024.3.5.) | http(s), "보도자료", 기관명+날짜 |

전수 추출이 원칙. 몇 건인지 먼저 세서 사용자에게 알리고 시작한다.

### 2. 유형별 검증 — 레시피는 references 참조

| 유형 | 라우팅 | 상세 |
|------|--------|------|
| 법령·판례 | k-skill 프록시 (키 불필요) → 법제처 직접(OC) 폴백 | [legal.md](references/legal.md) |
| 학술 문헌 | DOI→CrossRef 단건 / 없으면 CrossRef→OpenAlex→S2, 국문은 KCI 제목검색(학술지)→RISS(학위논문) | [scholarly.md](references/scholarly.md) |
| 웹/보도자료 | WebFetch → insane-search 스킬 → 죽은 링크는 Wayback CDX | insane-search 위임 |
| 통계 수치 | KOSIS 계열 인용이면 korean-stats-mcp 연결 시 대조 | 선택 |

검증 항목 (유형 공통 원칙):
1. **실존** — 그 문헌/조문/판례가 실제로 있는가
2. **서지 일치** — 저자·연도·법원·선고일·조문번호가 인용 표기와 맞는가
3. **내용 일치** — 논문이 그 출처에 귀속시킨 서술이 원문과 맞는가. 직접 인용문 "..."은 문자열 대조. **인용에 페이지 지정(pp.X–Y)이 있고 원문 PDF가 있으면** → §전문 대조로 그 페이지 내용까지 검증
4. **현행성** — 법령은 개정·시행일 체크 (집필 중 개정이 단골 사고)

### 3. 판정 규칙

- ✅ **확인** — 실존 + 서지 일치. 내용까지 대조했으면 명시
- ⚠️ **불일치** — 존재하지만 뭔가 다름: 조문 개정됨, 연도 오기, 저자 누락, 조항 번호 밀림, 판례가 상급심에서 뒤집힘. **무엇이 어떻게 다른지 반드시 병기**
- ❌ **미확인** — 환각 의심. 단, **최소 2개 독립 소스에서 미발견**이어야 ❌. 한 곳 miss로 단정 금지. 검색어 변형(제목 일부·저자·영문)도 시도한 뒤 판정

### 4. 리포트

```markdown
## 인용 검증 리포트 — {문서명} ({N}건)
✅ {n}  ⚠️ {n}  ❌ {n}

| # | 인용 표기 | 유형 | 판정 | 확인 내용 |
|---|-----------|------|------|-----------|
| 1 | 「지방재정법」 제38조 | 법령 | ⚠️ | 2026.1.15. 개정, 제1항 문구 변경 — 현행 조문: "..." |
...

### 조치 필요 항목
(⚠️·❌만 골라 수정 방향 제시)
```

⚠️·❌ 항목은 근거(조회 URL·응답 발췌)를 남긴다. 검증 못 한 항목(접근 불가 소스)은 "미검증"으로 솔직하게 분리 — 조용히 빼먹지 않는다.

## 전문 대조 모드 (페이지 단위 내용 검증)

"이 논문이 참고문헌의 **몇 페이지 내용을 참고했는지**"까지 검증할 때. 서지 실존(✅)을 넘어, 인용이 지목한 페이지의 실제 내용이 논문 주장을 뒷받침하는지 대조한다. **피인용 논문 원문 PDF가 있어야만 동작** — 없으면 서지 검증까지만 하고 "본문 미확보로 내용 미검증"으로 분리 보고.

### 절차

1. **매핑** — 검증 대상 논문의 각 인용(각주·본문 괄호)에서 `(저자, 연도, pp.X–Y)` 페이지 지정을 뽑고, 사용자가 준 PDF 파일과 짝짓는다. 파일-인용 대응이 모호하면 사용자에게 확인.
2. **페이지 오프셋 캘리브레이션** ⚠️ 함정 — **인쇄 페이지번호 ≠ PDF 물리 페이지번호**. 논문이 학술지 71–84쪽이면 PDF 1쪽이 인쇄 71쪽일 수 있고, 앞에 표지·목차가 붙으면 또 밀린다.
   - PDF를 Read `pages`로 앞 1–2장 열어 **실제 인쇄된 쪽번호(하단/상단)** 를 확인 → 오프셋 계산: `PDF물리 = 인용쪽 − 인쇄시작쪽 + 물리시작`
   - KCI API의 `<fpage>`로 그 논문 시작 인쇄쪽을 미리 알아두면 캘리브레이션이 빠름
3. **페이지 추출 · 대조** — 계산된 물리 페이지(경계 걸침 대비 ±1쪽 포함)를 Read `pages`로 열어 텍스트 확보 → 논문이 그 페이지에 귀속시킨 서술과 원문을 의미 단위로 대조.
4. **판정** (내용 대조 결과):
   - ✅ **뒷받침** — 지정 페이지에 해당 내용 있고 논문 서술과 일치
   - ⚠️ **과장/왜곡** — 있으나 논문이 원문보다 강하게/다르게 주장, 또는 맥락 절단
   - ⚠️ **페이지 어긋남** — 내용은 그 논문에 있으나 지정 페이지가 아닌 다른 쪽 (쪽 오기)
   - ❌ **부재** — 지정 페이지(±1)에 그 내용 없음 (인용 조작 의심)
   - 🔍 **미검증** — 원문 PDF 미확보 / 스캔 이미지라 텍스트 추출 불가(→ 사용자에 OCR본 요청)

### 함정 (실측 대비)
- **스캔 PDF**(이미지 논문)는 Read가 이미지로 인식 — 텍스트 대조 신뢰도 낮음, 판정에 명시
- **오프셋 미보정 = 오검증**: 캘리브레이션 없이 인쇄쪽=물리쪽으로 가정하면 엉뚱한 페이지를 대조해 ❌ 오판. 반드시 2단계 먼저
- 대량 PDF는 필요한 페이지 범위만 Read(전체 로드 금지)

## 탐색 모드 (근거 찾기)

"이 주장 뒷받침할 근거/선행연구/법적 근거 찾아줘" → 같은 라우팅을 역방향으로:
- 법적 근거: 프록시 `target=law` 키워드 검색 + `expc`(유권해석) + `prec`(판례)
- 선행연구: KCI·RISS(국문) + OpenAlex·CrossRef(영문) 키워드 검색, 결과는 서지 완전 표기(저자·연도·제목·저널·DOI)로 제공 — 그래야 바로 참고문헌에 붙임
- 찾은 근거도 실존 검증을 거친 것만 제시 (검색 결과 요약을 그대로 믿지 않는다)

## 주의

- Google Scholar는 봇벽 — 시도하지 말고 WebSearch로 대체
- PRISM(정책연구보고서)은 JS 벽 — 필요하면 deepcloak 스킬로만
- 법률 자문처럼 단정하지 않는다. 검증 결과 + 원문 출처까지만
- 대량(수십 건) 검증 시 소스별 rate limit 준수: [scholarly.md](references/scholarly.md) 하단 표
